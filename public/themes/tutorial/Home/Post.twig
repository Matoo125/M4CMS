{% extends 'Base.twig' %}

{% block body %}

<div class="container">
    <h1 class="my-4">{{ post.title}} </h1>
    
      <p class="lead">{{ post.description }}</p>
      {% if post.image %}
        <img src="/public/uploads/{{ post.image }}" class="img-fluid" alt="">
      {% endif %}
    
      <p>{{ post.content|raw }}</p>
</div>


{% endblock %}