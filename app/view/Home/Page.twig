{% extends 'Skeleton.twig' %}

{% block content %}
  <div class="header">
    <h1>{{ page.title}} </h1>
    <p class="lead">{{ page.description }}</p>
    <p>{{ page.content|raw }}</p>
  </div>

  <div>


    {% for category in categories %}
        <h4>{{ category.title }}</h4>
        {% if category.posts %}
         <!-- <ul class="article-list-thumbnails">
            {% for post in category.posts %}
              <li style="background-image: url('/public/uploads/{{ post.image }}')">
                <h2><a href="{{ page.slug }}/{{ category.slug }}/{{ post.slug }}">{{ post.title }}</a></h2>
              </li>
            {% endfor %}
          </ul>
        -->
        <div class="card">
            <img class="card-img-top img-fluid" src="http://via.placeholder.com/450x350" alt="Card image cap">
            <div class="card-block">
              <h4 class="card-title">Card title</h4>
              <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
            </div>
            <div class="card-footer">
              <small class="text-muted">Last updated 3 mins ago</small>
            </div>
          </div>
        {% endif %}
    {% endfor %}
  </div>

{% endblock %}
